# axentx-dev-bot decision
- id: `20260603-035641-dev-guide-E3-E3-S1-T2-973b87e1`
- project: dev-guide
- focus: feature
- created_at: 2026-06-03T03:56:41.195788Z

## dev — axentx-prd @ 2026-06-03T03:56:41.195873Z

Task derived from PRD 20260526-141000-devto-career-5d869138b8dbf224.

Story: As a developer, I want to see my learning progress, so I can stay motivated.
Acceptance:
  - Dashboard displays a progress bar for each active recommendation.
  - Progress updates automatically when a course is marked complete.
  - User can view a detailed progress report in a modal.

Task: Create progress API endpoint
Likely files: /opt/axentx/dev-guide/api/progress.py

Implement and produce a concrete code diff.

## dev — claude/llm-fallback-chain @ 2026-06-03T21:25:31.705295Z

### Diagnosis

The reviewer's feedback indicates that the previous attempt at implementing the progress API endpoint was rejected. The likely reasons for this rejection are:

1. **Lack of concrete implementation**: The previous attempt may not have provided a clear and actionable plan for implementing the progress API endpoint.
2. **Insufficient attention to detail**: The reviewer may have cited specific blockers that were not addressed in the previous attempt.

To resolve these blockers, I will provide a concrete implementation plan that includes specific files and lines, as well as actual code diffs.

### Proposed Change

* Update the `progress.py` file in the `/opt/axentx/dev-guide/api` directory to implement the progress API endpoint.
* Add a new endpoint to retrieve the user's progress data.
* Update the `models.py` file in the `/opt/axentx/dev-guide/api` directory to define the `Progress` model.
* Update the `serializers.py` file in the `/opt/axentx/dev-guide/api` directory to define the `ProgressSerializer` class.

### Implementation

#### progress.py
```diff
--- a/progress.py
+++ b/progress.py
@@ -1,0 +1,14 @@
+from rest_framework.response import Response
+from rest_framework.views import APIView
+from .models import Progress
+from .serializers import ProgressSerializer

+class ProgressView(APIView):
+    def get(self, request):
+        user_id = request.user.id
+        progress = Progress.objects.filter(user_id=user_id)
+        serializer = ProgressSerializer(progress, many=True)
+        return Response(serializer.data)
```

#### models.py
```diff
--- a/models.py
+++ b/models.py
@@ -1,0 +1,6 @@
+from django.db import models

+class Progress(models.Model):
+    user_id = models.IntegerField()
+    course_id = models.IntegerField()
+    completed = models.BooleanField(default=False)
```

#### serializers.py
```diff
--- a/serializers.py
+++ b/serializers.py
@@ -1,0 +1,14 @@
+from rest_framework import serializers
+from .models import Progress

+class ProgressSerializer(serializers.ModelSerializer):
+    class Meta:
+        model = Progress
+        fields = ('user_id', 'course_id', 'completed')
```

### Verification

To verify that the implementation is correct, I will perform the following steps:

1. Run the `python manage.py makemigrations` and `python manage.py migrate` commands to apply the changes to the database.
2. Use the `curl` command to test the new endpoint: `curl -X GET http://localhost:8000/api/progress/`
3. Verify that the response contains the user's progress data in JSON format.

By following these steps, I am confident that the implementation will resolve the blockers cited by the reviewer and provide a functional progress API endpoint.

## dev — dev @ 2026-06-03T21:25:31.705314Z

### Diagnosis

The reviewer's feedback indicates that the previous attempt at implementing the progress API endpoint was rejected. The likely reasons for this rejection are:

1. **Lack of concrete implementation**: The previous attempt may not have provided a clear and actionable plan for implementing the progress API endpoint.
2. **Insufficient attention to detail**: The reviewer may have cited specific blockers that were not addressed in the previous attempt.

To resolve these blockers, I will provide a concrete implementation plan that includes specific files and lines, as well as actual code diffs.

### Proposed Change

* Update the `progress.py` file in the `/opt/axentx/dev-guide/api` directory to implement the progress API endpoint.
* Add a new endpoint to retrieve the user's progress data.
* Update the `models.py` file in the `/opt/axentx/dev-guide/api` directory to define the `Progress` model.
* Update the `serializers.py` file in the `/opt/axentx/dev-guide/api` directory to define the `ProgressSerializer` class.

### Implementation

#### progress.py
```diff
--- a/progress.py
+++ b/progress.py
@@ -1,0 +1,14 @@
+from rest_framework.response import Response
+from rest_framework.views import APIView
+from .models import Progress
+from .serializers import ProgressSerializer

+class ProgressView(APIView):
+    def get(self, request):
+        user_id = request.user.id
+        progress = Progress.objects.filter(user_id=user_id)
+        serializer = ProgressSerializer(progress, many=True)
+        return Response(serializer.data)
```

#### models.py
```diff
--- a/models.py
+++ b/models.py
@@ -1,0 +1,6 @@
+from django.db import models

+class Progress(models.Model):
+    user_id = models.IntegerField()
+    course_id = models.IntegerField()
+    completed = models.BooleanField(default=False)
```

#### serializers.py
```diff
--- a/serializers.py
+++ b/serializers.py
@@ -1,0 +1,14 @@
+from rest_framework import serializers
+from .models import Progress

+class ProgressSerializer(serializers.ModelSerializer):
+    class Meta:
+        model = Progress
+        fields = ('user_id', 'course_id', 'completed')
```

### Verification

To verify that the implementation is correct, I will perform the following steps:

1. Run the `python manage.py makemigrations` and `python manage.py migrate` commands to apply the changes to the database.
2. Use the `curl` command to test the new endpoint: `curl -X GET http://localhost:8000/api/progress/`
3. Verify that the response contains the user's progress data in JSON format.

By following these steps, I am confident that the implementation will resolve the blockers cited by the reviewer and provide a functional progress API endpoint.

## review — reviewer @ 2026-06-04T00:04:15.994289Z

APPROVE: Incremental implementation of a progress API endpoint with model and serializer definitions.

- Verify that the new `ProgressView` is added to the project's URL configuration (e.g., `path('api/progress/', ProgressView.as_view())`) and that authentication middleware is enabled so `request.user` is populated.
- Run migrations (`manage.py makemigrations` and `manage.py migrate`) and confirm the `Progress` table is created with the expected columns.
- Test the endpoint with an authenticated user (e.g., using `curl -H "Authorization: Token <token>" http://localhost:8000/api/progress/`) and ensure the JSON response matches the `ProgressSerializer` fields.
- Ensure that only the requesting user's progress records are returned (no leakage of other users' data) and that the view handles the case where no records exist gracefully (e.g., returns an empty list).

## security-review — security-review @ 2026-06-04T00:21:28.183828Z

security PASS (findings=0)

## qa — qa @ 2026-06-04T13:00:08.166381Z

PASS: The request is within scope – a TDD test plan for the new **/progress** API endpoint.  

---

## 1. Acceptance Criteria

| # | Criterion | Measurable Target |
|---|-----------|-------------------|
| 1 | **Endpoint exists** | `GET /api/progress` returns HTTP 200 for authenticated users. |
| 2 | **Progress bar data** | Response JSON contains an array `recommendations` where each item has `id`, `title`, `completedLessons`, `totalLessons`, and `percentage` (0‑100). |
| 3 | **Automatic update** | When a lesson is marked complete via `POST /api/lessons/{id}/complete`, subsequent `GET /api/progress` reflects the new `completedLessons` and `percentage`. |
| 4 | **Modal data** | `GET /api/progress/{recId}` returns detailed report with `recId`, `title`, `lessons` (array of `{lessonId, title, status}`), and `overallPercentage`. |
| 5 | **Auth enforcement** | Unauthenticated requests receive HTTP 401. |
| 6 | **Rate limiting** | Exceeding 100 requests/min per user returns HTTP 429. |
| 7 | **Error handling** | Invalid `recId` returns HTTP 404 with `{error: "Recommendation not found"}`. |

---

## 2. Unit Tests (Python – pytest + FastAPI TestClient)

```python
# tests/unit/test_progress_api.py
import pytest
from fastapi.testclient import TestClient
from app.main import app  # FastAPI instance

client = TestClient(app)

@pytest.fixture
def auth_headers():
    return {"Authorization": "Bearer fake-jwt-token"}

def test_endpoint_exists(auth_headers):
    resp = client.get("/api/progress", headers=auth_headers)
    assert resp.status_code == 200

def test_progress_structure(auth_headers):
    resp = client.get("/api/progress", headers=auth_headers)
    data = resp.json()
    assert isinstance(data["recommendations"], list)
    for rec in data["recommendations"]:
        assert set(rec.keys()) == {"id", "title", "completedLessons", "totalLessons", "percentage"}
        assert 0 <= rec["percentage"] <= 100

def test_unauthenticated_access():
    resp = client.get("/api/progress")
    assert resp.status_code == 401

def test_invalid_recid(auth_headers):
    resp = client.get("/api/progress/999999", headers=auth_headers)
    assert resp.status_code == 404
    assert resp.json()["error"] == "Recommendation not found"

def test_rate_limit(auth_headers):
    for _ in range(101):
        resp = client.get("/api/progress", headers=auth_headers)
    assert resp.status_code == 429
```

---

## 3. Integration Tests

| Test | Scenario | Expected Result |
|------|----------|-----------------|
| **Happy Path 1** | User has 3 active recommendations, each with 5 lessons; completes 2 lessons in rec1. | `GET /api/progress` shows rec1 `percentage=40`, `completedLessons=2`. |
| **Happy Path 2** | User opens modal for rec2. | `GET /api/progress/rec2` returns detailed lesson list with correct statuses. |
| **Happy Path 3** | Mark last lesson of rec3 complete. | Subsequent progress shows rec3 `percentage=100`, `completedLessons=5`. |
| **Edge 1** | Mark a lesson that does not belong to the user. | `POST /api/lessons/{id}/complete` returns 403. |
| **Edge 2** | Request progress for a recommendation the user has no access to. | `GET /api/progress/{recId}` returns 403. |
| **Edge 3** | Simulate DB outage during progress fetch. | API returns 503 with `{error: "Service unavailable"}`. |

*Integration test script skeleton (pytest + httpx async client):*

```python
# tests/integration/test_progress_flow.py
import pytest, httpx
from app.main import app

@pytest.mark.asyncio
async def test_progress_flow():
    async with httpx.AsyncClient(app=app, base_url="http://test") as ac:
        # Authenticate
        token = "valid-token"
        headers = {"Authorization": f"Bearer {token}"}

        # 1. Get initial progress
        r1 = await ac.get("/api/progress", headers=headers)
        assert r1.status_code == 200
        data1 = r1.json()

        # 2. Complete a lesson
        r2 = await ac.post("/api/lessons/lesson-42/complete", headers=headers)
        assert r2.stat
