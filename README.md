# Recommendation Engine

A simple recommendation engine that generates personalized learning paths based on skill-gap analysis.

## Usage

1. Create a list of skill gaps and learning paths.
2. Initialize the recommendation engine with the skill gaps and learning paths.
3. Call the `generate_recommendations` method to get a list of recommended learning paths.

## Testing

Run the tests using `pytest` to ensure the recommendation engine is working correctly.
